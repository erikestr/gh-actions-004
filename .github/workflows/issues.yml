name: Issue Detector
on:
    issues:
        types: [opened]
jobs:
    issue-info:
        runs-on: ubuntu-latest
        steps:
            - name: Print Issue
              run: echo "${{ toJson(github) }}"
